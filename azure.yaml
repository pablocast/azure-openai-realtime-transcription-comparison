# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json
name: paschoaloto-realtime
metadata:
  template: paschoaloto-realtime@1.0.0
services:
  web:
    project: .
    host: containerapp
    language: docker
    docker:
      path: ./Dockerfile
      context: .
      remoteBuild: true
